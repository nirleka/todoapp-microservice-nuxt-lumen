Todo Service
============

# Init & run docker:
```
docker-compose up -d --build
```

# Remove docker:
```
docker-compose down
```

# Local Api URL
`http://localhost:8080/`

# Service List:
See docker-compose.yml
